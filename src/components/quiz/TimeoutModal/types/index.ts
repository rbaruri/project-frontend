export interface TimeoutModalProps {
  isOpen: boolean;
  onRetake: () => void;
}

export interface ModalClasses {
  modalOverlay: string;
  modalPanel: string;
  iconContainer: string;
  button: string;
} 